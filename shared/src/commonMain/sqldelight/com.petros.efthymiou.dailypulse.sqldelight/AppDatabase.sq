
CREATE TABLE Notification (
id INTEGER PRIMARY KEY AUTOINCREMENT,
title TEXT NOT NULL,
wu TEXT NOT NULL,
imageUrl TEXT NOT NULL,
milliSeconds INTEGER NOT NULL
);

selectAll:
SELECT * FROM Notification;

insertArticle:
INSERT INTO Notification (title, wu, imageUrl, milliSeconds) VALUES (?, ?, ?, ?);
